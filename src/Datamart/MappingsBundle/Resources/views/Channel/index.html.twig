{% extends "DatamartMappingsBundle::layout.html.twig" %}

{% block datamartmappings_body %}
    <div>
        {% for message in app.session.flashbag.get('notice') %}
            <p>Message flash : {{ message }}</p>
        {% endfor %}
    </div>
    <h2 class="col-md-8">Liste des mappings Channel</h2>
    <a href="{{ path('datamart_mappings_channel_add') }}"  class="btn btn-default float-right">
        <i class="glyphicon glyphicon-edit"></i>
        Ajouter un mapping Channel
    </a>
    <div class="col-md-12">
        {% if pagination.pages_count > 0 %}
        <table class="table table-bordered table-striped">
            <thead>
            <tr>
                <th>#</th>
                <th>Champ PMS</th>
                <th>Champ Mappé</th>
                <th>Modifier</th>
                <th>Supprimer</th>
            </tr>
            </thead>
            <tbody>
            {% for channelMapping in channelMappingsList %}
                <tr>
                    <th scope="row">{{ channelMapping.id }}</th>
                    <td>{{ channelMapping.originalField }}</td>
                    <td>{{ channelMapping.mappedField }}</td>
                    <td><a href="{{ path('datamart_mappings_channel_edit', {'id': channelMapping.id}) }}"><i class="glyphicon glyphicon-cog"></i></a></td>
                    <td><a href="{{ path('datamart_mappings_channel_delete', {'id': channelMapping.id}) }}"><i class="glyphicon glyphicon-trash"></i></a></td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
            {% include "DatamartCoreBundle::pagination.html.twig" %}
        {% else %}
            <p>Aucun mapping Channel</p>
        {% endif %}
    </div>
{% endblock %}