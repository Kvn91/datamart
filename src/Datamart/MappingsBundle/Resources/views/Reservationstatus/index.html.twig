{% extends "DatamartMappingsBundle::layout.html.twig" %}

{% block datamartmappings_body %}
    <div>
        {% for message in app.session.flashbag.get('notice') %}
            <p>Message flash : {{ message }}</p>
        {% endfor %}
    </div>
    <h2 class="col-md-8">Liste des mappings Reservations Status</h2>
    <a href="{{ path('datamart_mappings_reservationstatus_add') }}"  class="btn btn-default float-right">
        <i class="glyphicon glyphicon-edit"></i>
        Ajouter un mapping Reservations Status
    </a>
    <div class="col-md-12">
        {% if pagination.pages_count > 0 %}
        <table class="table table-bordered table-striped">
            <thead>
            <tr>
                <th>#</th>
                <th>Champ PMS</th>
                <th>Champ Mappé</th>
                <th>Hôtel</th>
                <th>Modifier</th>
                <th>Supprimer</th>
            </tr>
            </thead>
            <tbody>
            {% for reservationstatusMapping in reservationstatusMappingsList %}
                <tr>
                    <th scope="row">{{ reservationstatusMapping.id }}</th>
                    <td>{{ reservationstatusMapping.originalField }}</td>
                    <td>{{ reservationstatusMapping.mappedField }}</td>
                    <td>{{ roomtypeMapping.hotel.shortName }}</td>
                    <td><a href="{{ path('datamart_mappings_reservationstatus_edit', {'id': reservationstatusMapping.id}) }}"><i class="glyphicon glyphicon-cog"></i></a></td>
                    <td><a href="{{ path('datamart_mappings_reservationstatus_delete', {'id': reservationstatusMapping.id}) }}"><i class="glyphicon glyphicon-trash"></i></a></td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
            {% include "DatamartCoreBundle::pagination.html.twig" %}
        {% else %}
            <p>Aucun mapping Reservations Status</p>
        {% endif %}
    </div>
{% endblock %}